#include "main.h"
#include <ctype.h>

/**
 * _isupper - main function checks if integer is upper case
 *
 * @c: integer to be analysed
 * Return: Return 0 or 1
 */
int _isupper(int c)
{
	char chr;
	
	c = isupper(chr);
	if (c == 0)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
