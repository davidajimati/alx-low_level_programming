#include "main.h"
#include <stdio.h>

/**
 * mul - function to multiply 2 integers
 *
 * @a: integer a
 * @b: integer b
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = (a * b);
	printf("%d\n", mul);
	return (0);
}
