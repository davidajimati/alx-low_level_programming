#include "main.h"
#include <stdio.h>

/**
 * mul - function to multiply 2 integers
 *
 * @a: integer a
 * @b: integer b
 * Return: Always 0
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	printf("%d\n", result);
	return (0);
}
