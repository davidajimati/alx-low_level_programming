#include <stdio.h>
/**
 * main - Program entry point
 *
 * Return: Shows success status when it returns 0
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
